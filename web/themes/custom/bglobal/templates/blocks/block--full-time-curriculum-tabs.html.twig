{#
/**
 * @file
 * Theme override to display a block.
 *
 * Available variables:
 * - plugin_id: The ID of the block implementation.
 * - label: The configured label of the block if visible.
 * - configuration: A list of the block's configuration values.
 *   - label: The configured label for the block.
 *   - label_display: The display settings for the label.
 *   - provider: The module or other provider that provided this block plugin.
 *   - Block plugin specific settings will also be stored here.
 * - content: The content of this block.
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *   - id: A valid HTML ID and guaranteed unique.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @see template_preprocess_block()
 */
#}
<div{{ attributes }}>
  {{ title_prefix }}
  {{ title_suffix }}
  {% block content %}
    {% include '@organisms/curriculum-tabs/curriculum-tabs.twig' with {
      'program': {
        'shortName': content.bglobal.program["#shortName"],
        'certificate_type': content.bglobal.program['#certificate_type'],
        'cta': {
          'url': content.bglobal.hero['#cta'][0].uri,
          'text': content.bglobal.hero['#cta'][0].title,
        },
        'delivery': content.bglobal.program["#delivery"],
        'capstone': content.bglobal.program["#capstone"],
        'certificates': [{
            'name': content.bglobal.program['#certificates'][0].name,
            'description': content.bglobal.program['#certificates'][0].description,
            'prerequisites': content.bglobal.program['#certificates'][0].prerequisites,
            'offered': content.bglobal.program['#certificates'][0].offered,
            'length': content.bglobal.program['#certificates'][0].length,
            'benefits': content.bglobal.program['#certificates'][0].benefits,
            'outcomes': content.bglobal.program['#certificates'][0].outcomes,
            'courses': "<div certificate-courses data-cert='0'></div>",
            }, {
            'name': content.bglobal.program['#certificates'][1].name,
            'description': content.bglobal.program['#certificates'][1].description,
            'prerequisites': content.bglobal.program['#certificates'][1].prerequisites,
            'offered': content.bglobal.program['#certificates'][1].offered,
            'length': content.bglobal.program['#certificates'][1].length,
            'benefits': content.bglobal.program['#certificates'][1].benefits,
            'outcomes': content.bglobal.program['#certificates'][1].outcomes,
            'courses': "<div certificate-courses data-cert='1'></div>",
          }],
        }
      }%}
  {% endblock %}
</div>
